description: Analog Devices ADXL343 3-axis accelerometer driver sensor

compatible: "adi,adxl343"

include: ["i2c-device.yaml", "sensor-device.yaml"]

properties:
  reg:
    required: true
    description: device address on i2c bus